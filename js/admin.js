/**
 * Admin JavaScript
 *
 * Handles all the JavaScript needs of the admin area.
 *
 * @package 	Launchpad
 * @since   	Version 1.0
 */

jQuery(document).ready(
	function($) {
		// Do admin stuff.
	}
);
